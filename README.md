# Task Tracker Analysis: SQL анализ эффективности обработки тикетов

## Обзор проекта

Аналитический проект на SQL, исследующий эффективность обработки тикетов в системе трекинга задач.  
Проект демонстрирует полный цикл: от исследовательского анализа и проверки качества данных до расчёта ключевых метрик и выявления паттернов.

**Технологический стек:** PostgreSQL, SQL, VIEW, индексы

**Размер датасета:** 51,955 тикетов за исторический период

---

## Ключевые результаты

| Метрика | Значение |
|---------|----------|
| **Всего тикетов** | 51,950 |
| **Доля решённых** | 94.67% |
| **Решено в тот же месяц** | 95.48% |
| **Главная резолюция** | Fixed (95.17%) |
| **Основной тип обращений** | Local + Remote (85.72%) |

**Выводы:**
- Высокая эффективность обработки: практически все тикеты решаются быстро
- 2,766 открытых тикетов (5.33%) требуют пересмотра процесса работы с backlog
- Справочник резолюций избыточен: 171 из 176 записей не используются

---

## Структура проекта

```
task-tracker-analysis/
├── sql/
│   ├── 01_init_schema.sql         # Создание таблиц и индексов
│   ├── 02_data_cleaning.sql       # CREATE VIEW issues_clean (очищенный слой)
│   ├── 03_data_quality.sql        # EDA + проверки качества данных
│   └── 04_analysis.sql             # Основные расчёты и метрики (10 разделов)
├── docs/
│   ├── analyses_report.md         # Детальный аналитический отчёт (11 разделов)
│   ├── schema.md                  # Описание структуры данных
│   └── METHODOLOGY.md             # Подход и методология анализа
├── README.md                       # Этот файл
└── .gitignore
```

---

## Описание файлов

### SQL скрипты

**`sql/01_init_schema.sql`**  
Создание исходных таблиц `issues` и `resolutions`, а также индексов для оптимизации JOIN и фильтров.

**`sql/02_data_cleaning.sql`**  
Создание VIEW `issues_clean` — "чистого слоя" с преобразованиями:
- Конвертация Unix timestamp (мс) → дата/время
- Расчёт времени решения в днях
- Фильтрация epoch-аномалий

**`sql/03_data_quality.sql`**  
EDA и проверки качества:
- Анализ пропусков в данных
- Выявление временных аномалий (epoch, resolved < created, долгие тикеты)
- Проверка связности со справочником резолюций

**`sql/04_analysis.sql`**  
Основной анализ (10 разделов):
1. Sanity-checks витрины
2. Базовые метрики задания (same-month rate, распределение резолюций)
3. SLA-пороги (<=1/3/7/14/30 дней) по категориям
4. Распределение времени решения (квантили p50/p75/p90/p95)
5. Анализ "хвоста" долгих тикетов
6. Структура исходов: резолюции × категории (перекрёстный анализ)
7. Когортный анализ по месяцам создания
8. Временные паттерны (дни недели)
9. Backlog: анализ нерешённых тикетов (aging buckets)
10. Сводная витрина по категориям (все метрики в одном запросе)

### Документация

**`docs/analyses_report.md`**  
Полный аналитический отчёт (11 разделов):
1. Контекст и цель анализа
2. Описание данных
3. Проверка качества данных
4. Подготовка "чистого слоя"
5. Описательная статистика
6. Метрика "решено в тот же месяц"
7. Анализ резолюций и скорости обработки
8. Анализ backlog
9. Закономерности распределения категорий
10. Рекомендации по улучшению
11. Ограничения анализа

**`docs/schema.md`**  
Документация структуры данных с описанием всех полей, типов, ограничений.

---

## Быстрый старт

### Требования
- PostgreSQL 12+
- Доступ к БД с правами на создание таблиц и представлений

### Шаг 1: Загрузить данные
```sql
-- Загрузить CSV файлы в таблицы issues и resolutions
COPY issues FROM 'path/to/issues.csv' WITH (FORMAT CSV, HEADER);
COPY resolutions FROM 'path/to/resolutions.csv' WITH (FORMAT CSV, HEADER);
```

### Шаг 2: Создать схему и индексы
```bash
psql -U your_user -d your_db -f sql/01_init_schema.sql
```

### Шаг 3: Создать чистый слой
```bash
psql -U your_user -d your_db -f sql/02_data_cleaning.sql
```

### Шаг 4: Проверить качество данных
```bash
psql -U your_user -d your_db -f sql/03_data_quality.sql
```

### Шаг 5: Выполнить анализ
```bash
psql -U your_user -d your_db -f sql/04_analysis.sql
```

---

## Вопросы исследования

Проект ответил на следующие вопросы:

1. **Какова доля тикетов, решённых в тот же месяц, когда они были созданы?**  
   ➜ 95.48% (высокий показатель оперативности)

2. **Как распределены решённые тикеты по типам резолюций?**  
   ➜ Fixed (95.17%), WontFix (2.54%), resolvedByUser (1.61%), Duplicate (0.6%), escalated (0.08%)

3. **Как распределены категории тикетов и какие паттерны можно выявить?**  
   ➜ Local (50.75%) и Remote (34.97%) доминируют; New User (11.83%) может быть индикатором проблем онбординга

4. **Какие проблемы качества данных присутствуют?**  
   ➜ 5.32% пропусков в resolved (ожидаемо); 2 epoch-аномалии; 1 неправильная связь с резолюцией; 171 неиспользуемая резолюция

---

## Ключевые инсайты

### Положительные результаты
- **94.67%** всех тикетов решено (хороший показатель пропускной способности)
- **95.48%** решено в тот же месяц (стабильная и быстрая обработка)
- **Fixed** как основная резолюция (95.17%) говорит об эффективной работе
- **Стабильная нагрузка** без резких всплесков по дням недели

### Области для улучшения
- **2,766 открытых тикетов** требуют процесса работы с backlog
- **171 неиспользуемая резолюция** в справочнике (нужна очистка)
- **New User категория** (11.83% обращений) может быть связана с проблемами онбординга
- **SLA-метрики** не видны в текущих данных (рекомендуется добавить)

---

## Методология анализа

Проект следует научному подходу:

1. **EDA** (Exploratory Data Analysis) — первичное исследование структуры
2. **Data Quality Checks** — выявление пропусков, аномалий, нарушения связности
3. **Data Cleaning** — создание VIEW `issues_clean` с преобразованиями
4. **Metrics Calculation** — расчёт ключевых показателей
5. **Pattern Analysis** — выявление закономерностей и паттернов

**Ключевой принцип:** Перед расчётом метрик выполняется тщательная проверка качества данных, чтобы результаты были надёжными.

---

## Технические детали

### Используемые SQL техники
- **VIEW** — создание переиспользуемого "чистого слоя"
- **Window functions** — `ROW_NUMBER()`, `SUM() OVER (PARTITION BY ...)`
- **Aggregation** — `COUNT()`, `AVG()`, `PERCENTILE_CONT()`
- **Date functions** — `TO_TIMESTAMP()`, `DATE_TRUNC()`, `EXTRACT()`
- **FILTER clause** — условные агрегаты без CASE WHEN
- **CTE (Common Table Expression)** — структурирование сложных запросов
- **Индексы** — оптимизация JOIN по `resolution`, `category`, `created`, `resolved`

### Данные

| Таблица | Строк | Описание |
|---------|-------|----------|
| `issues` | 51,955 | Список тикетов с датами, категориями, резолюциями |
| `resolutions` | 176 | Справочник типов резолюций (используется только 5) |

**Поля `issues`:**
- `key` — идентификатор тикета
- `created` — Unix timestamp (мс) создания
- `resolved` — Unix timestamp (мс) решения (может быть NULL)
- `resolution` — ID типа резолюции (связь с `resolutions.id`)
- `category` — категория (Local, Remote, New User, Software License)

**Поля `resolutions`:**
- `id` — уникальный идентификатор
- `key` — название резолюции (fixed, wontFix, duplicate, escalated, resolvedByUser)

---

## 📝 Рекомендации

### Качество данных
- Ввести валидацию timestamp (исключить epoch, нулевые значения)
- Ввести контроль связности `issues.resolution → resolutions.id`
- Регулярно ревизовать справочник резолюций

### Процесс поддержки
- Внедрить процесс работы с backlog (triage, эскалация, cleanup)
- Для "New User" категории усилить онбординг и документацию
- Анализировать категории с высокой долей `duplicate` на предмет проблем поиска

### Метрики и мониторинг
- Использовать SLA-метрики по дням (<=1/3/7/14/30) вместо "same-month rate"
- Всегда показывать квантили (p50/p90/p95), а не только среднее
- Мониторить aging buckets открытых тикетов

---

## Ограничения анализа

В текущих данных отсутствуют поля:
- Приоритет тикета
- Команда/исполнитель
- SLA / дедлайн
- Текст/описание тикета
- Продукт/сервис

Это означает, что **причины различий можно формулировать как гипотезы**, но подтверждать их можно будет только при добавлении дополнительных атрибутов.

---

## Дополнительная информация

- **Полный отчёт:** `docs/analyses_report.md`
- **Описание схемы:** `docs/schema.md`
- **Все SQL запросы:** `sql/04_analysis.sql` (10 разделов)
